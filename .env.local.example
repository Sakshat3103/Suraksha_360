NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Maps run on free, keyless OpenStreetMap tiles + Nominatim search + OSRM
# routing (see src/lib/geo.ts) — no API key or billing account needed.

# Optional — leave blank to use built-in realistic mock AI responses instead
GEMINI_API_KEY=
